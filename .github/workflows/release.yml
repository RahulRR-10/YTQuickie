name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install Python deps
        run: |
          pip install -r backend/requirements.txt
          pip install pyinstaller

      - name: Download FFmpeg (ffmpeg.exe only — ffprobe is unused)
        shell: pwsh
        run: |
          $zip = "$env:RUNNER_TEMP\ffmpeg.zip"
          Invoke-WebRequest -Uri "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile $zip
          Expand-Archive -Path $zip -DestinationPath "$env:RUNNER_TEMP\ffmpeg"
          $ffmpeg = Get-ChildItem "$env:RUNNER_TEMP\ffmpeg" -Recurse -Filter "ffmpeg.exe" | Select-Object -First 1
          New-Item -ItemType Directory -Force -Path "release\ffmpeg" | Out-Null
          Copy-Item (Join-Path $ffmpeg.DirectoryName "ffmpeg.exe") "release\ffmpeg\ffmpeg.exe"
          Get-ChildItem release\ffmpeg | Select-Object Name, Length

      - name: Bundle Node.js runtime (YouTube JS-challenge solver)
        shell: pwsh
        run: |
          $node = (Get-Command node).Source
          Write-Host "Bundling Node from $node"
          New-Item -ItemType Directory -Force -Path "release\nodejs" | Out-Null
          Copy-Item $node "release\nodejs\node.exe"
          Get-ChildItem release\nodejs | Select-Object Name, Length

      - name: Compress bundled binaries with UPX
        shell: pwsh
        run: |
          choco install upx -y --no-progress
          $upx = (Get-Command upx).Source
          Write-Host "UPX at $upx"
          # UPX only packs binaries listed here; PyInstaller's upx flag does
          # not touch datas, so the staged ffmpeg/node exes are packed directly.
          & $upx --best --lzma "release\ffmpeg\ffmpeg.exe" "release\nodejs\node.exe"
          Get-ChildItem release\ffmpeg, release\nodejs | Select-Object Name, Length

      - name: Build with PyInstaller
        run: pyinstaller backend\YTQuickie.spec --noconfirm

      - name: Package zip
        shell: pwsh
        run: Compress-Archive -Path dist\YTQuickie -DestinationPath YTQuickie-windows-x64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: YTQuickie-windows-x64
          path: YTQuickie-windows-x64.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: YTQuickie-windows-x64.zip
          generate_release_notes: true